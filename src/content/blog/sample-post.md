---
title: "Sample post — layout preview (PLACEHOLDER)"
description: "Placeholder description so the blog layout is visible in dev. This post is draft:true and never ships to production, the sitemap, or the RSS feed. Delete it and write real posts."
pubDate: 2026-07-15
lang: en
draft: true
tags: ["placeholder"]
---

**This is placeholder content** — it exists only so you can see the post layout. It's `draft: true`, so it renders in `npm run dev` but is excluded from the production build, the sitemap, and RSS. Replace this whole file with real posts.

## A section heading

Lorem-ish body copy to show the prose column, paragraph rhythm, and max line length. The eyebrow above shows the publication date and an estimated reading time; the header, footer, and single-toggle language selector all come from the standard layout.

Because this post has no French translation, its language toggle points to the French blog index (`/fr/blogue`), and no hreflang pair is emitted — only a self-referencing canonical.

### A sub-heading

- A list item, to check list styling
- Another list item
- A third

> A blockquote, to confirm the quote styling in the prose column.

Some `inline code`, and a [link to the homepage](https://elevatek.ca/) to confirm link styling.
