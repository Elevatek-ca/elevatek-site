# elevatek.ca

Source for the Elevatek marketing site. Astro, deployed on Netlify.

`src/` — layouts, components, pages · `static-v2/` — pre-migration parity reference

`npm run dev` to work locally · `npm run build` to verify